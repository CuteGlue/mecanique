// Priorités des opérateurs
public class e06_priorites_operateurs {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		// a = (x+5)
		// a = (x=y) + 2
		// a = (x = (y + 2))
		// (a<b) && (c<d)
		// (i++) * (n+p)
		//  x += (n%p)
		// n = (p+=5) 
		int résultat;
		// a = x+5
		// a = (x=y) + 2
		// a = x = y + 2
		// a<b && c<d
		// i++ * (n+p)
		//  x += n%p
		// n = (p+=5)
		
		
		
		
		
		
		

	}

}
